<?xml version="1.0" encoding="utf-8"?>
<!-- had to change below <androidx.constraintlayout.widget.ConstraintLayout to <layout to use the viewmodel-->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <data>
        <variable
            name="viewModel"
            type="com.systematicshoppers.sysho.SyshoViewModel" />
    </data>

    <!-- FIXME do I need to constrain linearLayout in any way? -->
    <LinearLayout
        android:id="@+id/lin_layout"
        android:orientation="vertical">

        <TextView
            android:id="@+id/resultsDebugTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
             />

        <ScrollView
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/scroll_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/repeatList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </ScrollView>

    </LinearLayout>
</layout>